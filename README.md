Built a simple Flask web application and deployed it on an AWS EC2 Ubuntu instance. Configured security groups, Python virtual environment, and ran the app on port 5000 so that it can be accessed publicly using the instance’s public IP address. Code is maintained in a public GitHub repository.

